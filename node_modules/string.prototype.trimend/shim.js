'use strict';

var define = require('string.prototype.trimend/node_modules/define-properties');
var getPolyfill = require('string.prototype.trimend/polyfill');

module.exports = function shimTrimEnd() {
	var polyfill = getPolyfill();
	define(
		String.prototype,
		{ trimEnd: polyfill },
		{ trimEnd: function () { return String.prototype.trimEnd !== polyfill; } }
	);
	return polyfill;
};
